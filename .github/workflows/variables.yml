name: Workflow Demo

on: 
  push

env:
  ACCESS_ID: ${{ vars.ACCESS_ID }}
  SEC: ${{secrets.abc}}
jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
    - name: Echo ACCESS_ID
      run: |
        echo ${ACCESS_ID}
        echo ${{ vars.TEST }}
        echo ${{ secrets.abc}}
        echo ${{ env.SEC}}
        env
